CLICKHOUSE_DB=
CLICKHOUSE_USER=
CLICKHOUSE_PASSWORD=
DB_SEEDERS_FILE=/app/seeders/seed.csv

DB_DROP_EVERY_RELAUNCH=false # тестовый режим. при включении сбрасывает базу данных при каждом запуске сервиса

KAFKA_BROKERS=kafka:9092

KAFKA_BROKER_DOCKER=kafka:9092
KAFKA_BROKER_URL=kafka:9092
KAFKA_TOPIC=processed_data
PRODUCER_CLIENT_ID=228

GF_SECURITY_ADMIN_USER=
GF_SECURITY_ADMIN_PASSWORD=

# Public Domain Configuration
# This is the domain/IP where users will access your services
# 
# For LOCAL development:
#   HTTP_PUBLIC_DOMAIN=localhost:3000
#
# For PRODUCTION (with domain):
#   HTTP_PUBLIC_DOMAIN=your-domain.com
#   or with port: HTTP_PUBLIC_DOMAIN=your-domain.com:3000
#
# For PRODUCTION (with IP):
#   HTTP_PUBLIC_DOMAIN=192.168.1.100:3000
#   or HTTP_PUBLIC_DOMAIN=your.server.ip.address:3000
#
HTTP_PUBLIC_DOMAIN=localhost:3000
